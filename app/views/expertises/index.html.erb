<div class="row">
  <div class="col-md-12">
    <div class="panel panel-primary panel-table">
      <div class="panel-heading">
        <h1 class="panel-title text-center"><%= t('views.expertises.index.header_text') %></h1>
      </div>
      <div class="panel-body panel-body-table">
        <table class="table">
          <thead class="bg-info">
            <tr>
              <th class="text-center"><%= t('activerecord.attributes.expertise.name_en') %></th>
              <th class="text-center"><%= t('activerecord.attributes.expertise.name_es') %></th>
              <th class="text-center"><%= t('table.actions') %></th>
            </tr>
          </thead>

          <tbody>
            <% @expertises.each do |expertise| %>
              <tr>
                <td><h4><%= expertise.name_en %></h4></td>
                <td><h4><%= expertise.name_es %></h4></td>
                <td>
                  <%= link_to t('forms.buttons.delete'), expertise, method: :delete, class: "btn btn-primary pull-right btn-small-right-margin", data: { confirm: 'Are you sure?' } %>
                  <%= link_to t('forms.buttons.edit'), edit_expertise_path(expertise), class: "btn btn-primary pull-right btn-small-right-margin" %>
                  <%= link_to t('forms.buttons.show'), expertise, class: "btn btn-primary pull-right btn-small-right-margin" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <br>

        <%= link_to t('forms.buttons.new'), new_expertise_path, class: "btn btn-primary" %>
        <%= link_to t('forms.buttons.control_panel'), control_panel_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
