<div class="row">
  <div class="col-md-12">
    <div class="panel panel-default panel-table">
      <div class="panel-heading">
        <h1 class="panel-title text-center"><%= t('views.modalities.index.header_text') %></h1>
      </div>
      <div class="panel-body panel-body-table">
        <table class="table">
          <thead class="bg-info">
            <tr>
              <th class="text-center"><%= t('activerecord.attributes.modality.name_en') %></th>
              <th class="text-center"><%= t('activerecord.attributes.modality.name_es') %></th>
              <th class="text-center"><%= t('table.actions') %></th>
            </tr>
          </thead>

          <tbody>
            <% @modalities.each do |modality| %>
              <tr>
                <td><h4><%= modality.name_en %></h4></td>
                <td><h4><%= modality.name_es %></h4></td>
                <td>
                  <%= link_to t('forms.buttons.delete'), modality, method: :delete, class: "btn btn-primary pull-right btn-small-right-margin", data: { confirm: 'Are you sure?' } %>
                  <%= link_to t('forms.buttons.edit'), edit_modality_path(modality), class: "btn btn-primary pull-right btn-small-right-margin" %>
                  <%= link_to t('forms.buttons.show'), modality, class: "btn btn-primary pull-right btn-small-right-margin" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <br>

        <%= link_to t('forms.buttons.new'), new_modality_path, class: "btn btn-primary" %>
        <%= link_to t('forms.buttons.control_panel'), control_panel_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
