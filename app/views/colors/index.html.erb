<div class="row">
  <div class="col-md-12">
    <div class="panel panel-default panel-table">
      <div class="panel-heading">
        <h1 class="panel-title text-center"><%= t('views.colors.index.header_text') %></h1>
      </div>
      <div class="panel-body panel-body-table">
        <table class="table">
          <thead class="bg-info">
            <tr>
              <th class="text-center"><%= t('activerecord.attributes.color.name_en') %></th>
              <th class="text-center"><%= t('activerecord.attributes.color.name_es') %></th>
              <th class="text-center"><%= t('table.actions') %></th>
            </tr>
          </thead>

          <tbody>
            <% @colors.each do |color| %>
              <tr>
                <td><h4><%= color.name_en %></h4></td>
                <td><h4><%= color.name_es %></h4></td>
                <td>
                  <%= link_to t('forms.buttons.delete'), color, method: :delete, class: "btn btn-primary pull-right btn-small-right-margin", data: { confirm: 'Are you sure?' } %>
                  <%= link_to t('forms.buttons.edit'), edit_color_path(color), class: "btn btn-primary pull-right btn-small-right-margin" %>
                  <%= link_to t('forms.buttons.show'), color, class: "btn btn-primary pull-right btn-small-right-margin" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <br>

        <%= link_to t('forms.buttons.new'), new_color_path, class: "btn btn-primary" %>
        <%= link_to t('forms.buttons.control_panel'), control_panel_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>