<% if @photo.errors.any? %>
	$("#modal-photo-info-container").html("<%= j render 'errors' %>");
<% else %>
	$("photo-<%= @album.id %><%= @photo.id %>").remove();
	$("#profile-modal").modal("hide");
<% end %>