<%= form_for(review) do |f| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fromable_id %>
    <%= f.text_field :fromable_id %>
  </div>

  <div class="field">
    <%= f.label :toable_id %>
    <%= f.text_field :toable_id %>
  </div>

  <div class="field">
    <%= f.label :review_en %>
    <%= f.text_area :review_en %>
  </div>

  <div class="field">
    <%= f.label :review_es %>
    <%= f.text_area :review_es %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
