<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-navbar-collapse" aria-expanded="false">
		        <span class="sr-only">Toggle navigation</span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		    </button>
			<%= link_to root_path, class: "navbar-brand" do %>
				<%= image_tag "cubamodela_logo_blue.png", width:"90", alt: "Logo" %>
			<% end %>
		</div>

		<div class="collapse navbar-collapse" id="bs-navbar-collapse">
			<ul id="navbar-ul" class="nav navbar-nav">
				<li><a href="#models-container"><%= t('menu.items.models') %></a></li>
				<li><a href="#photographers-container"><%= t('menu.items.photographers') %></a></li>
				<li><a href="#us-container"><%= t('menu.items.us') %></a></li>
				<li><a href="#gallery-home-container"><%= t('menu.items.gallery') %></a></li>
			</ul>
			<% if user_signed_in? %>
				<% if current_user.model? %>
					<%= link_to t('menu.options.find_casting'), castings_path, class: "btn btn-primary navbar-btn" %>
				<% end %>
				<% if current_user.admin? %>
					<%= link_to t('menu.options.control_panel'), control_panel_path, class: "btn btn-primary navbar-btn" %>
				<% end %>
				<%= render 'layouts/signed_in_true' %>
			<% else %>
				<%= render 'layouts/signed_in_false' %>
			<% end %>
		</div>
	</div>
</nav>