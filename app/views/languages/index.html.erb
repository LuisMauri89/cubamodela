<div class="row">
  <div class="col-md-12">
    <div class="panel panel-primary panel-table">
      <div class="panel-heading">
        <h1 class="panel-title text-center"><%= t('views.languages.index.header_text') %></h1>
      </div>
      <div class="panel-body panel-body-table">
        <table class="table">
          <thead class="bg-info">
            <tr>
              <th class="text-center"><%= t('activerecord.attributes.language.name_en') %></th>
              <th class="text-center"><%= t('activerecord.attributes.language.name_es') %></th>
              <th class="text-center"><%= t('table.actions') %></th>
            </tr>
          </thead>

          <tbody>
            <% @languages.each do |language| %>
              <tr>
                <td><h4><%= language.name_en %></h4></td>
                <td><h4><%= language.name_es %></h4></td>
                <td>
                  <%= link_to t('forms.buttons.delete'), language, method: :delete, class: "btn btn-primary pull-right btn-small-right-margin", data: { confirm: 'Are you sure?' } %>
                  <%= link_to t('forms.buttons.edit'), edit_language_path(language), class: "btn btn-primary pull-right btn-small-right-margin" %>
                  <%= link_to t('forms.buttons.show'), language, class: "btn btn-primary pull-right btn-small-right-margin" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <br>

        <%= link_to t('forms.buttons.new'), new_language_path, class: "btn btn-primary" %>
        <%= link_to t('forms.buttons.control_panel'), control_panel_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
